<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:id="@+id/carddd"
    android:longClickable="true"
     app:cardElevation="@dimen/_10sdp"
    android:layout_margin="@dimen/_5sdp"
    app:cardCornerRadius="@dimen/_10sdp">


<androidx.constraintlayout.widget.ConstraintLayout

    android:layout_width="@dimen/_130sdp"
    android:id="@+id/itemConstraint"
    android:clickable="true"
    android:longClickable="true"
    android:layout_height="match_parent"

   >
    <ImageView
        android:layout_width="@dimen/_100sdp"
        android:layout_height="@dimen/_140sdp"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:id="@+id/libImage">
    </ImageView>
    <ImageView
        android:layout_width="@dimen/_30sdp"
        android:layout_height="@dimen/_30sdp"
        android:id="@+id/checkButton"
        android:src="@drawable/checkmark"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"

        app:layout_constraintTop_toTopOf="@id/libImage">

    </ImageView>
    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="TEST"
        android:maxLines="2"
        android:ellipsize="end"
        android:id="@+id/libText"
        android:textSize="@dimen/_15ssp"
        android:textColor="@color/black"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@id/libImage"
        app:layout_constraintStart_toStartOf="@id/libImage"
        app:layout_constraintTop_toBottomOf="@id/libImage"
       >

    </TextView>
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:clickable="true"
        android:visibility="visible"
        android:longClickable="true"
        android:id="@+id/frame"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent">
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>